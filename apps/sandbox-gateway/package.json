{
  "name": "sandbox-gateway",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@openplane/sandbox": "workspace:*",
    "dotenv": "catalog:"
  },
  "devDependencies": {
    "@openplane/config": "workspace:*",
    "@types/bun": "^1.2.6",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
