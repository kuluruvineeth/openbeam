---
title: Auth & Team
description: User, Session, Account, Team, UsersOnTeam, UserInvite, ApiKey
---

<ExcalidrawViewer
  src="/diagrams/excalidraw/er-auth-team.excalidraw"
  caption="Auth & Team ER diagram — 7 tables with all fields, types, relationships, and indices"
  height={750}
/>

## Models

### User
Core identity model. 8 scalar fields, 18 relations.

| Field | Type | Constraints |
|-------|------|------------|
| `id` | String | PK, cuid |
| `name` | String | |
| `email` | String | Unique |
| `emailVerified` | Boolean | |
| `image` | String? | |
| `teamId` | String? | FK → Team |
| `createdAt` | DateTime | Default: now() |
| `updatedAt` | DateTime | @updatedAt |

**Relations:** sessions (1:N), accounts (1:N), usersOnTeam (1:N), connectors (1:N), externalIdentities (1:N), groupMemberships (1:N), feedback (1:N), agentCanvases (1:N), agentCanvasExecutions (1:N), agentCanvasSessions (1:N)

### Team
Multi-tenant organization boundary. All data is team-scoped.

| Field | Type | Constraints |
|-------|------|------------|
| `id` | String | PK, cuid |
| `name` | String | |
| `slug` | String | Unique |
| `logo` | String? | |
| `settings` | Json | Default: {} |
| `maxConnectors` | Int | Default: 10 |
| `maxTools` | Int | Default: 100 |
| `subscriptionTier` | String | Default: "free" |
| `subscriptionStatus` | String | Default: "active" |

**Relations:** 30+ relations spanning all domain models

### UsersOnTeam
Many-to-many with role assignment.

| Field | Type | Constraints |
|-------|------|------------|
| `userId` | String | FK → User |
| `teamId` | String | FK → Team |
| `role` | TeamRole | OWNER, ADMIN, MEMBER |

### ApiKey
Scoped API access with hash-based lookup.

| Field | Type | Constraints |
|-------|------|------------|
| `id` | String | PK |
| `teamId` | String | FK → Team |
| `name` | String | |
| `keyHash` | String | Unique |
| `prefix` | String | |
| `scopes` | String[] | Default: ["read", "write"] |
| `expiresAt` | DateTime? | |
| `revoked` | Boolean | Default: false |
