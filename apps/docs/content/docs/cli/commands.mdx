---
title: Command Reference
description: Complete reference for every openbeam CLI command, organized by resource.
---

# Command Reference

Every command follows the pattern `openbeam <resource> <action> [flags]`. Mutation commands (create, delete, sync) require `--yes` in non-interactive mode.

---

## search

Search across all indexed content. 8 subcommands covering text search, hybrid retrieval, media, and author-based queries.

### search query

Search documents with filters across connector types, date ranges, labels, and ranking strategies.

```bash
openbeam search query --query "deployment checklist" --limit 10
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--query`, `-q` | string | | Search query text |
| `--connector-type` | string[] | | Filter by connector type (repeatable) |
| `--connector-id` | string | | Filter by connector ID |
| `--document-type` | string[] | | Filter by document type (repeatable) |
| `--source-type` | string[] | | Filter by source type (repeatable) |
| `--status` | string[] | | Filter by status (repeatable) |
| `--priority` | string[] | | Filter by priority level (repeatable) |
| `--label` | string[] | | Filter by label (repeatable) |
| `--author-id` | string | | Filter by author ID |
| `--source-id` | string | | Filter by source ID |
| `--from-date` | int64 | `0` | Filter results after this timestamp |
| `--to-date` | int64 | `0` | Filter results before this timestamp |
| `--limit` | int | `20` | Maximum results to return |
| `--offset` | int | `0` | Results offset for pagination |
| `--ranking` | string | `hybrid` | Ranking strategy |

```bash
# Search Slack messages from a specific connector
openbeam search query -q "launch plan" --connector-type slack --limit 5

# Search with date range
openbeam search query -q "Q4 report" --from-date 1696118400 --to-date 1704067200
```

### search recent

Get recently indexed documents.

```bash
openbeam search recent --hours 12 --limit 50
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--hours` | int | `24` | Lookback window in hours |
| `--limit` | int | `20` | Maximum results |

### search thread

Retrieve all messages in a thread.

```bash
openbeam search thread --thread-id "thr_abc123"
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--thread-id` | string | | Yes |

### search similar

Find documents similar to a given document.

```bash
openbeam search similar --document-id "doc_xyz" --limit 5
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--document-id` | string | | Yes |
| `--limit` | int | `10` | No |

### search author

Get all documents by a specific author.

```bash
openbeam search author --author-id "usr_abc" --limit 25
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--author-id` | string | | Yes |
| `--limit` | int | `50` | No |

### search media

Search indexed media (video, audio) with type-specific filters.

```bash
openbeam search media -q "product demo" --media-type video --limit 10
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--query`, `-q` | string | | Search query text |
| `--connector-id` | string | | Filter by connector ID |
| `--source-id` | string | | Filter by source ID |
| `--media-type` | string | | Filter by media type |
| `--from-date` | int64 | `0` | Start timestamp |
| `--to-date` | int64 | `0` | End timestamp |
| `--limit` | int | `20` | Maximum results |
| `--offset` | int | `0` | Pagination offset |
| `--ranking` | string | `hybrid` | Ranking strategy |

### search unified

Search across both documents and media in a single request.

```bash
openbeam search unified -q "architecture overview" --include-media --include-documents
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--query`, `-q` | string | | Search query text |
| `--include-documents` | bool | `true` | Include documents |
| `--include-media` | bool | `true` | Include media |
| `--connector-type` | string[] | | Filter by connector type |
| `--connector-id` | string | | Filter by connector ID |
| `--document-type` | string[] | | Filter by document type |
| `--source-id` | string | | Filter by source ID |
| `--from-date` | int64 | `0` | Start timestamp |
| `--to-date` | int64 | `0` | End timestamp |
| `--limit` | int | `20` | Maximum results |
| `--offset` | int | `0` | Pagination offset |
| `--ranking` | string | `hybrid` | Document ranking strategy |
| `--media-ranking` | string | `hybrid` | Media ranking strategy |

### search hybrid

Advanced hybrid search with configurable RRF (Reciprocal Rank Fusion) weights for BM25, dense, and sparse retrieval.

```bash
openbeam search hybrid -q "machine learning pipeline" --weight-bm25 0.3 --weight-dense 0.5 --weight-sparse 0.2
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--query`, `-q` | string | | Search query text |
| `--mode` | string | `hybrid_v2` | Hybrid search mode |
| `--rrf-k` | int | `60` | RRF constant k parameter |
| `--weight-bm25` | float64 | `0.4` | BM25 ranking weight |
| `--weight-dense` | float64 | `0.4` | Dense vector ranking weight |
| `--weight-sparse` | float64 | `0.2` | Sparse vector ranking weight |
| `--connector-type` | string[] | | Filter by connector type |
| `--document-type` | string[] | | Filter by document type |
| `--source-id` | string[] | | Filter by source ID |
| `--from-date` | int64 | `0` | Start timestamp |
| `--to-date` | int64 | `0` | End timestamp |
| `--limit` | int | `20` | Maximum results |
| `--offset` | int | `0` | Pagination offset |

---

## auth

Manage API keys, check auth status, and handle the credential lifecycle.

### auth login

Store an API key for the active profile. Verifies the key against the server by default.

```bash
openbeam auth login --api-key "op_your_key_here"
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--api-key` | string | | API key value |
| `--skip-verify` | bool | `false` | Skip server verification |

### auth logout

Remove the stored API key for the active profile.

```bash
openbeam auth logout
```

### auth status

Show current authentication status.

```bash
openbeam auth status --verify
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--verify` | bool | `false` | Verify the key against the server |

### auth token print

Print the active API key (redacted by default).

```bash
openbeam auth token print
openbeam auth token print --redact=false
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--redact` | bool | `true` | Redact the token output |

### auth key list

List API keys for a team.

```bash
openbeam auth key list --team-id "team_abc"
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--team-id` | string | | Team identifier (defaults to active profile team) |

### auth key create

Create a new API key.

```bash
openbeam auth key create --name "CI Pipeline" --scope read:search --scope write:connectors
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--name` | string | | Yes |
| `--team-id` | string | | No (uses profile team) |
| `--scope` | string[] | | No (repeatable) |
| `--expires-at` | string | | No (RFC3339 format) |

### auth key revoke

Revoke an existing API key.

```bash
openbeam auth key revoke --id "key_abc123" --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |
| `--team-id` | string | | No |

---

## connectors

Manage connector installations, trigger syncs, and view sync history. 12 subcommands.

### connectors list

List all connector apps and their installation status.

```bash
openbeam connectors list
```

### connectors get

Get detailed information about a connector.

```bash
openbeam connectors get --id "conn_abc123"
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |

### connectors connect

Create a new connector installation.

```bash
openbeam connectors connect \
  --app-id "gmail" \
  --workspace-external-id "ws_123" \
  --name "Company Gmail" \
  --auth-type OAUTH2 \
  --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--app-id` | string | | Yes |
| `--workspace-external-id` | string | | Yes |
| `--name` | string | | Yes |
| `--type` | string | `SOURCE` | No |
| `--auth-type` | string | `OAUTH2` | No |
| `--config` | string | `{}` | No (JSON object) |

### connectors update

Update connector settings.

```bash
openbeam connectors update --id "conn_abc" --config '{"syncInterval":"1h"}' --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |
| `--config` | string | `{}` | No (JSON object) |

### connectors disconnect

Schedule a connector for disconnection.

```bash
openbeam connectors disconnect --id "conn_abc" --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |

### connectors sync

Trigger a sync for a connector.

```bash
openbeam connectors sync --id "conn_abc" --type FULL --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |
| `--type` | string | `FULL` | No |

### connectors status

Get the current sync status of a connector.

```bash
openbeam connectors status --id "conn_abc"
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |

### connectors history

Get sync history for a connector.

```bash
openbeam connectors history --id "conn_abc" --limit 10
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |
| `--limit` | int | `20` | No |
| `--offset` | int | `0` | No |

### connectors pause

Pause a running connector.

```bash
openbeam connectors pause --id "conn_abc" --yes
```

### connectors resume

Resume a paused connector.

```bash
openbeam connectors resume --id "conn_abc" --yes
```

### connectors resources

List resources available for a connector (channels, repos, drives, etc.).

```bash
openbeam connectors resources --id "conn_abc" --search "engineering" --limit 50
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |
| `--search` | string | | No |
| `--cursor` | string | | No |
| `--limit` | int | `50` | No |

### connectors resource-sync

Toggle syncing for a specific resource.

```bash
openbeam connectors resource-sync --resource-id "res_abc" --enabled=true --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--resource-id` | string | | Yes |
| `--enabled` | bool | `true` | No |

---

## agent

Execute AI agents and stream their responses.

### agent run

Execute an agent request and receive the complete result.

```bash
openbeam agent run --prompt "Find all documents about our pricing strategy" --agent-type rag
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--prompt`, `-p` | string | | Yes |
| `--agent-type` | string | `rag` | No |
| `--max-steps` | int | `10` | No |

### agent stream

Stream agent events in real-time via SSE.

```bash
openbeam agent stream --prompt "Analyze our competitor landscape" --max-steps 15
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--prompt`, `-p` | string | | Yes |
| `--agent-type` | string | `rag` | No |
| `--max-steps` | int | `10` | No |

---

## rag

Retrieval-Augmented Generation workflows. Ask questions, stream answers, and manage conversations.

### rag ask

Ask a question grounded in your indexed data.

```bash
openbeam rag ask --query "What were the key decisions from the last board meeting?"
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--query`, `-q` | string | | Yes |
| `--conversation-id` | string | | No |
| `--model-id` | string | | No |
| `--temperature` | float64 | `0` | No |
| `--include-media` | bool | `true` | No |
| `--source-id` | string | | No |

### rag stream

Stream a RAG response token by token.

```bash
openbeam rag stream --query "Explain our authentication architecture"
```

Flags are identical to `rag ask`.

### rag conversations create

Create a new conversation for multi-turn RAG.

```bash
openbeam rag conversations create --yes
```

### rag conversations list

List existing conversations.

```bash
openbeam rag conversations list --status active --limit 10
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--status` | string | `active` | Conversation status |
| `--limit` | int | `20` | Page size |
| `--offset` | int | `0` | Pagination offset |

### rag conversations get

Get conversation details and message history.

```bash
openbeam rag conversations get --id "conv_abc"
```

### rag conversations delete

Delete a conversation.

```bash
openbeam rag conversations delete --id "conv_abc" --yes
```

---

## research

Orchestrate long-running research workflows powered by AI agents.

### research start

Start a research workflow with a prompt.

```bash
openbeam research start --prompt "Analyze our market position vs Glean" --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--prompt` | string | | Yes |
| `--max-steps` | int | | No |

### research progress

Check progress of a running research workflow.

```bash
openbeam research progress --workflow-id "wf_abc123"
```

### research artifacts

Retrieve artifacts produced by a research workflow.

```bash
openbeam research artifacts --workflow-id "wf_abc123"
```

### research cancel

Cancel a running research workflow.

```bash
openbeam research cancel --workflow-id "wf_abc123" --yes
```

---

## canvas

Manage agent canvases -- visual agent workflow orchestration. Create, publish, and execute multi-step agent pipelines.

### canvas list

```bash
openbeam canvas list --status published --limit 10
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--status` | string | | Canvas status filter |
| `--limit` | int | `20` | Page size |
| `--offset` | int | `0` | Pagination offset |

### canvas create

```bash
openbeam canvas create --name "Lead Qualification" --nodes '[...]' --edges '[...]' --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--name` | string | | Yes |
| `--description` | string | | No |
| `--icon` | string | | No |
| `--nodes` | string | `[]` | No (JSON array) |
| `--edges` | string | `[]` | No (JSON array) |
| `--viewport` | string | | No (JSON object) |
| `--settings` | string | | No (JSON object) |
| `--trigger-type` | string | | No |
| `--trigger-config` | string | | No (JSON object) |

### canvas get

```bash
openbeam canvas get --id "cnv_abc"
```

### canvas update

```bash
openbeam canvas update --id "cnv_abc" --name "Updated Name" --yes
```

Flags are identical to `canvas create`, plus `--id` (required).

### canvas delete

```bash
openbeam canvas delete --id "cnv_abc" --yes
```

### canvas publish

Publish a canvas version.

```bash
openbeam canvas publish --id "cnv_abc" --changelog "Added retry logic" --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |
| `--changelog` | string | | No |

### canvas executions

List executions for a canvas.

```bash
openbeam canvas executions --id "cnv_abc" --status completed --limit 10
```

### canvas execute

Trigger a new canvas execution.

```bash
openbeam canvas execute --id "cnv_abc" --input '{"query":"analyze pipeline"}' --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |
| `--input` | string | | No (JSON object) |
| `--trigger-source` | string | | No |
| `--session-id` | string | | No |
| `--turn-id` | string | | No |

---

## background-agents

Manage long-running background agents for autonomous task execution.

### background-agents list

```bash
openbeam background-agents list --status running --limit 10
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--status` | string | | Agent status filter |
| `--limit` | int | `20` | Page size |
| `--offset` | int | `0` | Pagination offset |

### background-agents get

```bash
openbeam background-agents get --id "ba_abc"
```

### background-agents create

```bash
openbeam background-agents create \
  --name "Code Reviewer" \
  --prompt "Review all PRs for security issues" \
  --preset researcher \
  --sandbox-type daytona \
  --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--name` | string | | Yes |
| `--prompt` | string | | Yes |
| `--description` | string | | No |
| `--preset` | string | `researcher` | No |
| `--sandbox-type` | string | `daytona` | No |
| `--repository-url` | string | | No |
| `--base-branch` | string | | No |
| `--timeout-ms` | int | `0` | No |

### background-agents pause

```bash
openbeam background-agents pause --id "ba_abc" --yes
```

### background-agents resume

```bash
openbeam background-agents resume --id "ba_abc" --from-checkpoint 3 --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |
| `--from-checkpoint` | int | `0` | No |

### background-agents cancel

```bash
openbeam background-agents cancel --id "ba_abc" --yes
```

### background-agents delete

```bash
openbeam background-agents delete --id "ba_abc" --yes
```

### background-agents logs

View and follow agent execution logs.

```bash
# Get recent logs
openbeam background-agents logs --id "ba_abc" --limit 100

# Follow logs in real-time
openbeam background-agents logs --id "ba_abc" --follow --interval 1s
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--id` | string | | Agent identifier (required) |
| `--level` | string | | Log level filter |
| `--limit` | int | `100` | Page size |
| `--offset` | int | `0` | Pagination offset |
| `--follow` | bool | `false` | Follow logs in real-time |
| `--interval` | duration | `2s` | Follow poll interval |

---

## knowledge

Query the knowledge graph -- entities, relations, topics, and expertise.

### knowledge list

```bash
openbeam knowledge list --type person --limit 20
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--type` | string | | Entity type filter |
| `--cursor` | string | | Pagination cursor |
| `--limit` | int | `20` | Page size |

### knowledge search

```bash
openbeam knowledge search --query "machine learning" --type topic
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--query`, `-q` | string | | Yes |
| `--type` | string | | No |
| `--limit` | int | `10` | No |

### knowledge get

```bash
openbeam knowledge get --id "ent_abc"
```

### knowledge relations

Get entity relationships.

```bash
openbeam knowledge relations --id "ent_abc" --direction both
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--id` | string | | Yes |
| `--direction` | string | `both` | No |

### knowledge panel

Get a rich knowledge panel for an entity.

```bash
openbeam knowledge panel --id "ent_abc"
```

### knowledge experts

Find subject matter experts for a topic.

```bash
openbeam knowledge experts --topic-id "topic_ml" --limit 5
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--topic-id` | string | | Yes |
| `--limit` | int | `10` | No |

### knowledge expertise

Get a person's areas of expertise.

```bash
openbeam knowledge expertise --person-id "person_abc" --limit 10
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--person-id` | string | | Yes |
| `--limit` | int | `10` | No |

---

## media

Deep operations on indexed media -- transcripts, chapters, highlights, and Q&A.

### media chapters

```bash
openbeam media chapters --vespa-id "vid_abc"
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--vespa-id` | string | | Vespa document ID (required) |
| `--force-refresh` | bool | `false` | Regenerate content |

### media highlights

```bash
openbeam media highlights --vespa-id "vid_abc"
```

### media transcript

```bash
openbeam media transcript --vespa-id "vid_abc"
```

### media summary

```bash
openbeam media summary --vespa-id "vid_abc"
```

### media metadata

```bash
openbeam media metadata --vespa-id "vid_abc"
```

### media regenerate

Regenerate derived content for a media item.

```bash
openbeam media regenerate --vespa-id "vid_abc" --content-type chapters --content-type highlights --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--vespa-id` | string | | Yes |
| `--content-type` | string[] | | Yes (repeatable) |

### media ask

Ask a question about a specific media item.

```bash
openbeam media ask --media-id "vid_abc" --question "What was discussed at timestamp 5:30?"
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--media-id` | string | | Yes |
| `--question` | string | | Yes |

---

## teams

Manage teams and roles.

### teams list

```bash
openbeam teams list
```

### teams create

```bash
openbeam teams create --name "Engineering" --slug "engineering" --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--name` | string | | Yes |
| `--slug` | string | | Yes |

### teams switch

Switch the active team.

```bash
openbeam teams switch --id "team_abc" --yes
```

### teams role

Get your role for a specific team.

```bash
openbeam teams role --id "team_abc"
```

---

## permissions

Manage permission sync, caches, and access controls.

### permissions sync-status

```bash
openbeam permissions sync-status --connector-id "conn_abc"
```

### permissions sync-statuses

```bash
openbeam permissions sync-statuses
```

### permissions invalidate

Invalidate the permission cache.

```bash
openbeam permissions invalidate --scope all --yes
openbeam permissions invalidate --scope user --user-id "usr_abc" --yes
openbeam permissions invalidate --scope connector --connector-id "conn_abc" --yes
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--scope` | string | `all` | Invalidation scope: `user`, `connector`, `all` |
| `--user-id` | string | | User identifier |
| `--connector-id` | string | | Connector identifier |

### permissions me

Get your current permission set.

```bash
openbeam permissions me
```

### permissions document

Get permission details for a document.

```bash
openbeam permissions document --document-id "doc_abc"
```

### permissions groups

Get user group memberships.

```bash
openbeam permissions groups --user-id "usr_abc"
```

### permissions scopes

Get user connector scopes.

```bash
openbeam permissions scopes --user-id "usr_abc"
```

### permissions stats

Get team-wide permission statistics.

```bash
openbeam permissions stats
```

---

## analytics

Cost tracking, usage trends, and spreadsheet analytics.

### analytics cost-breakdown

```bash
openbeam analytics cost-breakdown --start-date 2025-01-01 --end-date 2025-01-31 --group-by model
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--start-date` | string | | Yes (ISO 8601) |
| `--end-date` | string | | Yes (ISO 8601) |
| `--group-by` | string[] | | No (repeatable) |

### analytics usage-trend

```bash
openbeam analytics usage-trend --start-date 2025-01-01 --end-date 2025-03-01 --granularity week
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--start-date` | string | | Yes |
| `--end-date` | string | | Yes |
| `--granularity` | string | `day` | No (`hour`, `day`, `week`, `month`) |

### analytics top-cost-drivers

```bash
openbeam analytics top-cost-drivers --start-date 2025-01-01 --end-date 2025-01-31 --dimension model --limit 5
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--start-date` | string | | Yes |
| `--end-date` | string | | Yes |
| `--dimension` | string | `model` | No |
| `--limit` | int | `10` | No |

### analytics spreadsheets list

```bash
openbeam analytics spreadsheets list --limit 20
```

### analytics spreadsheets schema

```bash
openbeam analytics spreadsheets schema --document-id "doc_abc" --sheet "Sheet1"
```

### analytics spreadsheets generate-sql

Generate SQL from a natural language question about a spreadsheet.

```bash
openbeam analytics spreadsheets generate-sql \
  --document-id "doc_abc" \
  --question "What is the total revenue by region?"
```

### analytics spreadsheets query

Execute a SQL query against a spreadsheet.

```bash
openbeam analytics spreadsheets query \
  --document-id "doc_abc" \
  --sql "SELECT region, SUM(revenue) FROM data GROUP BY region" \
  --view-name "revenue_view" \
  --max-rows 500
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--document-id` | string | | Yes |
| `--sql` | string | | Yes |
| `--view-name` | string | | Yes |
| `--max-rows` | int | `1000` | No |
| `--timeout-ms` | int | `10000` | No |

---

## integrations

OAuth and service account authentication flows for connectors.

### integrations oauth-start

Start an OAuth authorization flow.

```bash
openbeam integrations oauth-start --integration gmail --connector-id "conn_abc"
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--integration` | string | | Yes (`gmail`, `google-drive`, `linear`, `notion`, `slack`) |
| `--connector-id` | string | | Yes |
| `--workspace-id` | string | | No |
| `--redirect-url` | string | | No |

### integrations oauth-callback

Complete an OAuth flow with the authorization code.

```bash
openbeam integrations oauth-callback \
  --integration gmail \
  --code "auth_code_here" \
  --state "state_param" \
  --yes
```

### integrations service-account-auth

Authenticate a Google service account connector.

```bash
openbeam integrations service-account-auth \
  --integration gmail \
  --connector-id "conn_abc" \
  --credentials '{"type":"service_account",...}' \
  --delegated-email "admin@company.com" \
  --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--integration` | string | | Yes (`gmail` or `google-drive`) |
| `--connector-id` | string | | Yes |
| `--credentials` | string | | No (service account JSON) |
| `--delegated-email` | string | | No |

---

## mcp

Model Context Protocol server and API exploration.

### mcp serve

Start the MCP JSON-RPC server over stdin/stdout.

```bash
openbeam mcp serve
```

### mcp tools list

List all available MCP tools.

```bash
openbeam mcp tools list
```

### mcp tools call

Call an MCP tool by name with JSON arguments.

```bash
openbeam mcp tools call --name "search_hybrid" --args '{"query":"test"}'
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--name` | string | | Yes |
| `--args` | string | `{}` | No |

### mcp resources list

```bash
openbeam mcp resources list
```

### mcp resources read

```bash
openbeam mcp resources read --uri "openbeam://connectors/list"
```

### mcp prompts list

```bash
openbeam mcp prompts list
```

### mcp prompts get

```bash
openbeam mcp prompts get --name "search_prompt" --args '{"query":"test"}'
```

---

## config

Manage CLI profiles and settings.

### config show

Show the active profile configuration.

```bash
openbeam config show
```

### config set

Set values on the active profile.

```bash
openbeam config set --host https://openbeam.example.com --team "team_abc" --timeout 60s
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--host` | string | | API server URL |
| `--team` | string | | Default team identifier |
| `--output` | string | | Output format (`table`, `json`, `yaml`, `ndjson`) |
| `--color` | string | | Color mode (`auto`, `always`, `never`) |
| `--timeout` | string | | Request timeout (e.g., `30s`, `2m`) |

### config use

Switch the active profile.

```bash
openbeam config use production
```

### config profiles

List all available profiles.

```bash
openbeam config profiles
```

---

## daemon

Manage the local OpenBeam daemon process.

### daemon start

```bash
openbeam daemon start
```

### daemon stop

```bash
openbeam daemon stop
```

### daemon status

```bash
openbeam daemon status
```

### daemon restart

```bash
openbeam daemon restart
```

### daemon pair

Pair the local daemon with a remote OpenBeam instance.

```bash
openbeam daemon pair --host https://openbeam.example.com
```

The daemon also includes subcommands for managing agent sessions, permissions, AI providers, speech processing, and worktrees. Run `openbeam daemon --help` for the full list.

---

## plugin

Manage CLI plugins.

### plugin list

List discovered plugins from all search directories.

```bash
openbeam plugin list
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--search-dir` | string[] | | Override search directories |

### plugin install

Install a plugin binary from a local path.

```bash
openbeam plugin install --name my-tool --source ./my-tool-binary --yes
```

| Flag | Type | Default | Required |
|------|------|---------|----------|
| `--name` | string | | Yes |
| `--source` | string | | Yes |
| `--dir` | string | | No (target directory) |

### plugin run

Run a plugin command directly.

```bash
openbeam plugin run my-tool --arg1 value
```

---

## version

Print the CLI version, commit hash, build date, and Go version.

```bash
openbeam version
# 0.1.0 (commit=abc1234 date=2025-02-15 go=go1.24.2 darwin/arm64)
```

---

## completion

Generate shell completion scripts.

```bash
openbeam completion bash
openbeam completion zsh
openbeam completion fish
openbeam completion powershell
```

See the [CLI overview](/docs/cli) for setup instructions per shell.
