---
title: MCP Server
description: Connect OpenBeam to Claude, Cursor, Windsurf, Codex, and any MCP-compatible AI tool.
---

# MCP Server

OpenBeam ships a Model Context Protocol (MCP) server that gives AI assistants access to your enterprise knowledge — 88 connector integrations with write actions, hybrid search, RAG answers, rich UI views, and connector management.

## Quick Setup

### Claude Desktop (Recommended)

Click the **+** button in any chat, then **Connectors > Manage Connectors > Add custom server**:

```
URL: https://api.openbeam.work/mcp
```

Claude will prompt you to sign in via OAuth. Or add to `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "openbeam": {
      "url": "https://api.openbeam.work/mcp",
      "headers": {
        "Authorization": "Bearer op_live_your_key_here"
      }
    }
  }
}
```

### Claude Code

```bash
claude mcp add --transport http openbeam https://api.openbeam.work/mcp
```

### Cursor

Open **Settings > Tools & MCP > Add new MCP server**, or add to `.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "openbeam": {
      "url": "https://api.openbeam.work/mcp",
      "headers": {
        "Authorization": "Bearer op_live_your_key_here"
      }
    }
  }
}
```

### Other Clients

All modern MCP clients support Streamable HTTP natively:

```
https://api.openbeam.work/mcp
```

See the [client setup guides](/docs/mcp/clients) for Windsurf, Codex, ChatGPT, VS Code, Continue, Cline, OpenCode, and Conductor.

## What You Get

Once connected, your AI assistant can:

- **Search** across all connected data sources with hybrid semantic + keyword search
- **Ask questions** with RAG-powered answers and citations
- **Manage connectors** — list, inspect health, connect new sources via OAuth
- **Trigger syncs** — run full or incremental syncs and monitor progress
- **Execute actions** — create issues, send messages, update records across 88 integrations
- **Browse context** — access the enterprise knowledge graph and context memory

## Full Documentation

<Cards>
  <Card title="MCP Overview" href="/docs/mcp" description="Architecture, capabilities, and tool catalog" />
  <Card title="Authentication" href="/docs/mcp/authentication" description="OAuth 2.1, API keys, and scope management" />
  <Card title="Client Guides" href="/docs/mcp/clients/claude-desktop" description="Step-by-step setup for 10 AI clients" />
  <Card title="Roadmap" href="/docs/mcp/roadmap" description="Upcoming MCP capabilities and integrations" />
</Cards>
