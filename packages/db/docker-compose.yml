name: openbeam

services:
  postgres:
    image: postgres
    container_name: openbeam-postgres
    environment:
      POSTGRES_DB: openbeam
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - openbeam_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  openbeam_postgres_data:
