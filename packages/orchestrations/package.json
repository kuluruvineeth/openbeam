{
  "name": "@openplane/orchestrations",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    },
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "check": "biome check --write .",
    "check-types": "tsc -b",
    "test": "bun test"
  },
  "dependencies": {
    "@openplane/db": "workspace:*",
    "@openplane/temporal": "workspace:*",
    "@openplane/types": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@openplane/config": "workspace:*",
    "tsdown": "catalog:"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
