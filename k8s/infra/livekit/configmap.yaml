apiVersion: v1
kind: ConfigMap
metadata:
  name: livekit-config
  namespace: openplane
data:
  livekit.yaml: |
    port: 7880
    rtc:
      port_range_start: 50000
      port_range_end: 60000
      use_external_ip: false
    keys:
      devkey: devsecret
    logging:
      level: info
    room:
      empty_timeout: 300
      max_participants: 50
