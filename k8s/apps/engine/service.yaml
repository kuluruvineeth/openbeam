apiVersion: v1
kind: Service
metadata:
  name: engine-cpu
  namespace: openplane
spec:
  type: NodePort
  selector:
    app: engine-cpu
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800
